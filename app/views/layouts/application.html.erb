<!DOCTYPE html>
<html>
  <head>
    <title>TodoAppV2</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="navbar navbar-expand-sm navbar-light bg-light">
      <div class="container">
        <% if logged_in? %>
          <%= link_to "TodoApp", todos_path, class: "navbar-brand" %>
          <ul class="navbar-nav">
            <li class="nav-item">
              <%= link_to user_path(current_user.id), class: "nav-link" do %>
                <%= image_tag url_for(current_user.image[0]), class: "", style: "#{image_style(20)}" %> <%= current_user.name %>
              <% end %>
            </li>
            <li class="nav-item"><%= link_to "ログアウト", logout_path, method: :delete, class: "nav-link" %></li>
          </ul>
          <% else %>
          <%= link_to "TodoApp", root_path, class: "navbar-brand" %>
          <ul class="navbar-nav">
            <li class="nav-item"><%= link_to "新規登録", new_user_path, class: "nav-link" %></li>
            <li class="nav-item"><%= link_to "ログイン", login_form_path, class: "nav-link" %></li>
          </ul>
        <% end %>
      </div>
    </header>
    <div class="container">
      <%= notice_msg %>
      <%= alert_msg %>
      <%= yield %>
    </div>
  </body>
</html>
